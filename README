Toolchain
---------

CLI for common SEO operations.

Installation
------------

Open your terminal, assuming you have Python3 installed, you can run

  python3 -m pip install https://github.com/wttrz/toolchain/archive/refs/heads/main.zip

Use of virtual environments is optional.
